package ch05.lecture.p02reference;

public class C01Reference {
	public static void main(String[] args) {
		//	기본타입(primitive type)
		//	참조타입(reference type)
		
		
		// 참조타입은 대략 객체다.
		//	>>> 참조타입은 사실 주소다. 그 주소가 가르키는 것이 객체(instance)	
			// 참조타입은 기본타입을 제외한 모든타입
			// 참조타입은 값이 아니라 주소로 저장됨
			// 	... js에서는 모든것이 다 객체였던것과 다른점인듯?
			// 객체의 속성에 접근하는법은 : '.'		
			// *****참조타입이 어떻게 저장되는 방식을 아는 것은 매우 중요!
				
		
		int a;
		
		
	}
}
